class Map{
    int dim_x, dim_y;
    Enemy enemies[];
    Segment segments[];
    int trigger_start, trigger_end;

    Map()
    Map(...){
        for i in range n
            random segment
        for i in range n
            random enemy
        
    }
    bool isSolid(int x, int y)
    int getX()
    int getY()
    toString()
}